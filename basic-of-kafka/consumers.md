consumers:

client application
reads messages from topics
connection pooling
network protocol
horizontally and elastically scalable
maintains ordering within partitions at scale




we can scale consumers instance.